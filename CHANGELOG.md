# 更新日志

## v0.4 - 开发中

### 新增

- 增加 `PistolVisualController`，集中处理手枪视觉反馈和可选操作音效接口。
- 运行时自动创建原型套筒、弹匣、扳机和抛壳口节点。
- 射击时播放套筒后退和复位动画，并抛出可见弹壳。
- 手动拉枪机退出膛内弹时抛出弹壳。
- 打空后套筒保持后拉，表现空仓挂机。
- 插拔弹匣时播放弹匣插入和下落动画。
- 扳机视觉会跟随扣动和松开状态变化。
- 为拉枪机、释放枪机、插拔弹匣和空仓挂机预留独立音效槽位。

### 说明

- 当前视觉模型仍是运行时生成的原型几何体。
- 后续替换成真实手枪模型时，只需要把真实套筒、弹匣、扳机和抛壳口节点赋给 `PistolVisualController`。

## v0.3 - 2026-06-01

### 新增

- 训练阶段记录有效射击次数、空击次数和误操作次数。
- 普通换弹从拔出旧弹匣开始计时，恢复可射击后记录最快换弹时间。
- 打空后进入空仓挂机时开始计时，完成换弹并恢复可射击后记录空仓处理时间。
- 结果页增加枪械操作统计。

### 优化

- 已插入弹匣时再次插入弹匣会提示先拔出，并记录为误操作。
- 射击阶段之外的调试按键不会污染训练统计。
- 枪机后拉、空仓挂机等无法射击状态也会播放轻微机械点击反馈。

## v0.2 - 2026-06-01

### 新增

- 增加独立弹药 HUD，实时显示弹匣剩余数量、膛内状态和枪机状态。
- 增加短暂操作提示，覆盖插拔弹匣、拉动枪机、释放枪机和解除空仓挂机。
- 增加无法射击原因提示，区分膛内无弹、无弹匣、枪机未释放、空仓挂机和扳机未复位。
- 当没有配置外部空击音频素材时，自动生成轻微空击占位声。
- 训练界面、武器 HUD 和操作反馈改为中文，并使用项目内置的 Noto Sans CJK SC 开源字体。

### 优化

- 手枪状态机的操作方法会返回操作是否真正生效，方便后续接入 VR 和自制枪型手柄反馈。
- 原有分数 HUD 与武器状态 HUD 分离，信息更容易阅读。
- 增强自动生成的空击占位声，使机械点击更清晰。

## v0.1 - 2026-05-29

这是项目进入拟真射击模拟方向后的第一个阶段版本，重点是把当前桌面射击原型整理成更适合继续扩展 VR 和自制枪型手柄的基础架构。

### 新增

- 增加第一版手枪状态机，支持弹匣、膛内弹、拉枪机、释放枪机、空仓挂机、空击和扳机复位逻辑。
- 增加 `WeaponProfile` 和 `WeaponFireMode`，为后续添加不同枪械预留通用配置。
- 增加 `IWeaponInput` 输入抽象接口。
- 增加 `MouseKeyboardWeaponInput` 键鼠调试输入，当前按键为：鼠标左键扣扳机，`R` 插入弹匣，`T` 拔出弹匣，`F` 拉枪机，`G` 释放枪机，`V` 释放空仓挂机。
- 增加中文开发计划文档，明确 VR、枪型手柄、拟真枪械、弹道、后坐力和训练系统的发展路线。

### 优化

- `GunShooter` 改为通过手枪状态机和输入接口触发射击，减少对鼠标键盘的直接耦合。
- 保留 G17 类手枪的 300 RPM 射速限制和短输入缓冲，改善连续点击时第二枪被吞的问题。
- 在 HUD 中显示武器状态，方便调试弹药和手枪状态。
- 调整运行时 UI 主题尺寸，让武器状态文本更容易阅读。
- 场景中的 Gun 对象已挂载手枪状态机和键鼠输入组件。

### 当前限制

- 目前仍是桌面调试版本，还没有真正接入 VR 输入。
- 手枪状态机已经有逻辑，但还没有对应的枪械模型动画和完整操作音效。
- 弹道仍是 Raycast 原型，还没有加入散布、材质反馈和可视化弹道。
- 自制枪型手柄和真实后坐力反馈尚未接入。
